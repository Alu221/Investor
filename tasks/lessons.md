# Ошибки и уроки: ИНвестбот

> Claude ОБЯЗАН записывать сюда каждую ошибку сразу после обнаружения.
> Claude ОБЯЗАН читать этот файл в начале каждой сессии.

## Когда записывать:
- Пользователь поправил Claude
- Claude сам нашёл баг в своём коде
- Тесты упали из-за кода Claude
- Пришлось переделывать из-за неверного подхода
- Забыл учесть требование из ТЗ

## Формат: одна строка = одна ошибка

| Дата | Ошибка | Решение | Правило на будущее |
|------|--------|---------|-------------------|
| 2026-03-22 | Перепутал задачу — спросил про прайс-листы вместо инвестбота | Уточнил у пользователя | Всегда уточнять контекст при неожиданном повороте |
| 2026-03-24 | max_turns=5 обрезал анализ — бот мог прочитать только 5 файлов knowledge/ | Поставил max_turns=25 по инструкции оператора | Всегда проверять дефолтные значения CLI-параметров |
| 2026-03-24 | OAuth токен протухает каждые несколько дней, бот молча умирает | Fallback на Anthropic API + обновление токена через чат + уведомление админу | OAuth токены ВСЕГДА протухают — нужен fallback |
| 2026-03-24 | _read_fresh_token() искал в несуществующем файле ~/.claude/.credentials.json | Оператор подтвердил: токен ТОЛЬКО в env var, не в файлах | Не гадать где хранятся credentials — спросить оператора |
| 2026-03-24 | Принимал на веру "токен живёт год" без проверки | Поставил healthcheck каждые 6 часов — узнаем когда реально протухнет | НЕ доверять непроверенным фактам, всегда ставить мониторинг |
| 2026-03-24 | claude setup-token не работает на Windows (localhost ERR_CONNECTION_REFUSED) | Известный баг. Решение: брать токен от iia-бота или пробовать с VPN/отключенным брандмауэром | Windows + localhost callback = проблема, искать обходной путь |
| 2026-03-24 | --resume со старой сессией давал 401 даже с рабочим токеном | Рестарт бота сбрасывает сессии (в памяти) — новый запрос идёт без resume | При смене токена ВСЕГДА перезапускать бота |
